/**
 * Tree-sitter runtime backend — public entry point
 *
 * Used by generated server bundles to create the runtime backend:
 *   import { TreeSitterRuntime } from 'treelsp/backend/tree-sitter';
 */

export { TreeSitterRuntime } from '../../runtime/parser/tree-sitter/backend.js';
