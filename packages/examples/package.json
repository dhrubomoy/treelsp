{
  "name": "examples",
  "version": "0.0.1",
  "private": true,
  "description": "Example languages for treelsp",
  "type": "module",
  "scripts": {
    "build": "echo 'No build needed'",
    "test": "vitest run",
    "lint": "eslint .",
    "clean": "rm -rf */generated"
  },
  "dependencies": {
    "treelsp": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
