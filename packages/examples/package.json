{
  "name": "examples",
  "version": "0.0.1",
  "private": true,
  "description": "Example languages for treelsp",
  "type": "module",
  "scripts": {
    "build": "treelsp generate && treelsp build",
    "test": "vitest run",
    "lint": "eslint .",
    "clean": "rm -rf */generated */generated-lezer"
  },
  "dependencies": {
    "treelsp": "workspace:*"
  },
  "devDependencies": {
    "@treelsp/cli": "workspace:*",
    "typescript": "^5.7.3",
    "vitest": "^4.0.18"
  }
}
