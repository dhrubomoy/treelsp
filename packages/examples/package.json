{
  "name": "examples",
  "version": "0.0.1",
  "private": true,
  "description": "Example languages for treelsp",
  "type": "module",
  "scripts": {
    "build": "cd mini-lang && node ../../cli/dist/index.js build",
    "test": "vitest run",
    "lint": "eslint .",
    "clean": "rm -rf */generated"
  },
  "dependencies": {
    "treelsp": "workspace:*"
  },
  "devDependencies": {
    "@treelsp/cli": "workspace:*",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
